# User Guide

## Start the App

Run:

```powershell
uv run takeoff-pro
```

The Phase 0 application opens an empty desktop window titled "Takeoff Pro". Project import, viewing, measuring, estimating, and report export will arrive in later phases.
