# Data Model

Phase 0 defines the package boundaries only. The native project model and legacy `Data.xml` mapping will be documented in Phase 1 after representative read-only job folders are inspected.

No real client data belongs in this repository. Synthetic fixtures must be used for automated tests.
